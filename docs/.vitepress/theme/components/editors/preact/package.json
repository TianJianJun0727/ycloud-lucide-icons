{
  "main": "index.js",
  "scripts": {
    "start": "npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "dependencies": {
    "@ycloud-web/icons-preact": "latest",
    "preact": "10.11.3",
    "preact-compat": "3.19.0"
  },
  "devDependencies": {
    "preact-cli": "^1.4.1"
  }
}
