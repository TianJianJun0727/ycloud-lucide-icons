name: YCloud Icons Static Checks

on:
  pull_request:
    paths:
      - icons/**
      - business-icons/**
      - illustration-icons/**
      - scripts/generateBusinessIconsPackage.mts
      - scripts/generateIllustrationsPackage.mts
      - scripts/writeAssetMetadata.mts
      - scripts/writeBusinessIconIndex.mts
      - scripts/writeIllustrationIndex.mts
      - scripts/render/**
      - packages/icons-static/**
      - tools/build-icons/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  icons-static:
    name: YCloud Icons Static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm --filter @ycloud-web/icons-static build
